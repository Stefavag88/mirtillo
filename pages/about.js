import React from 'react'
import Layout from '../components/Layout';

export default (props) => {
    
    return (
        <Layout url={props.url}>
            <p>About Page</p>
        </Layout>
    );
}
